cmake_minimum_required(VERSION 2.8.12)
project(roman_model)

find_package(catkin REQUIRED)

catkin_package()

install(DIRECTORY control   DESTINATION ${CATKIN_PACKAGE_SHARE_DESTINATION})
install(DIRECTORY gazebo    DESTINATION ${CATKIN_PACKAGE_SHARE_DESTINATION})
install(DIRECTORY materials DESTINATION ${CATKIN_PACKAGE_SHARE_DESTINATION})
install(DIRECTORY meshes    DESTINATION ${CATKIN_PACKAGE_SHARE_DESTINATION})
install(DIRECTORY robots    DESTINATION ${CATKIN_PACKAGE_SHARE_DESTINATION})
install(DIRECTORY urdf      DESTINATION ${CATKIN_PACKAGE_SHARE_DESTINATION})
